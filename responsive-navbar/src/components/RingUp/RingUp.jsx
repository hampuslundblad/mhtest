import React from "react";
import PropTypes from "prop-types";

import BreakTitle from "../Reusable/BreakTitle/BreakTitle";
import PrimaryTitle from "../Reusable/PrimaryTitle/PrimaryTitle";

const RingUp = (props) => {
  return (
    <div>
      <BreakTitle titleColor="#263271">Vi är medlem av RingUp</BreakTitle>
    </div>
  );
};

RingUp.propTypes = {};

export default RingUp;
